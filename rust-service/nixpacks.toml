# Nixpacks configuration for Rust scoring service

[phases.build]
nixPkgs = ["rustc", "cargo"]

[phases.install]
cmds = ["cargo build --release"]

[start]
cmd = "./target/release/rust-service"

[variables]
NIXPACKS_METADATA = "rust"
RUST_LOG = "info"